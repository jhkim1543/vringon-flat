/**
 * 파이프라인 코드 판. **손으로 올린다** — 실행에 영향을 주는 변경을 했으면 여기도 올린다.
 * 산출물에 박혀서, 나중에 "같은 사진인데 결과가 다르다"를 짚을 근거가 된다.
 *
 * 여기 한 곳뿐이다. 예전엔 run4.ts 와 cleanFinish.ts 가 각자 문자열을 들고 있었고,
 * v7.9.1 에서 정리 단계를 고쳤을 때 run 쪽만 올라가 산출물에 두 판이 같이 찍혔다 —
 * 판을 박아두는 목적이 바로 그걸 짚는 것인데 스탬프끼리 어긋나면 짚을 수가 없다.
 */
export const CODE_VERSION = "v7.9.1";
