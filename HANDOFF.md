# VRINGON FLAT — 인수인계

사진 한 장을 **레이어가 나뉜 플랫 스케치 `.ai`** 로 바꾼다. 2026-09-10 기준 파이프라인 판은 **v7.9.1**.

이 문서는 zip 을 처음 여는 사람을 위한 것이다. 두 가지를 적는다: **데모가 지금 어떤 상태인가**와
**어떤 방법으로 개발해 왔는가**. 알고리즘의 실측 이력 전문은 `docs/V6.md`(§1~§23)에 있다.

---

## 1. 데모

**https://demo.rebuilderai.com/flat**

`main` 에 push 하면 GitHub Actions → Elastic Beanstalk 로 자동 배포된다(8~12분). 데모 코드는
별도 저장소 `vringon-beta-module` 에 있고, 이 저장소의 `server/` 를 그쪽 `backend/flat/server` 로
복사해 쓴다(동기화 스크립트 `tools/flat-sync.sh`).

### 무엇을 볼 수 있나

| | |
| --- | --- |
| **샘플 4종** | 미리 굽어 둔 결과. 즉시 열리고 앵커 편집기·`.ai` 3벌 내려받기까지 된다 |
| **사진 업로드** | 실제로 파이프라인을 돌린다. 한 장에 보통 2~4분 |
| **앵커 편집기** | 브라우저에서 앵커를 잡아 옮기고 다시 `.ai` 로 굽는다 |

### 걸려 있는 샘플 넷

3중 게이트를 모두 통과한 셋에, 파트가 가장 풍부한 신발을 더한 조합이다.

| 샘플 | 앵커 | 선 F@2 | 게이트 | 레이어 |
| --- | --- | --- | --- | --- |
| `shoe_1` | 955 | 0.9903 | 충실도·편집성 | outsole · midsole · vamp · quarter · backcounter · toecap · tongue · eyestay · logo · laces · heeltap (11) |
| `bag_2` | 1,081 | 0.9950 | **3중 통과** | panel_front · chain · unclassified |
| `bag_3` | 4,225 | 0.9994 | **3중 통과** | panel_front · strap · chain |
| `jewelry_1` | 284 | 0.9924 | **3중 통과** | body · logo(각인 글자 9개) |

레이어 이름이 업계 어휘인 것은 우연이 아니다 — 사내 세그 모델의 학습된 클래스 이름을 그대로
쓴다(2절). 실행마다 같은 이름이 나오고, 사내 다른 기능(테크팩·CMF)과 어휘가 맞는다.

### API

인증은 `X-Api-Key`(쓰기 엔드포인트에만, `FLAT_API_KEYS` 로 설정). 전문은 `docs/openapi.yaml`.

| 엔드포인트 | 용도 |
| --- | --- |
| `GET /api/flat/status` | 어떤 백엔드가 붙어 있는지(키 값은 안 돌려준다, 있는지만) |
| `GET /api/flat/samples` | 샘플 목록 |
| `GET /api/flat/samples/{id}` | 파트 목록 · 캔버스 · 3중 QA · 파일 URL |
| `GET /api/flat/samples/{id}/file/{name}` | `production.svg` · `layered.ai` · `preview.png` … |
| `POST /api/flat/jobs` | 사진 업로드 → `{id}` |
| `GET /api/flat/jobs/{id}` | 진행 단계 · 로그 · 완료 시 파일 URL |

### 알아 둘 것 둘

- **실행 기록은 서버 메모리에만 있다.** 배포가 겹치면(다른 실험이 push 해도) 인스턴스가 교체되며
  진행 중인 잡이 사라진다. 화면은 24초를 견딘 뒤 "이 잡을 찾을 수 없어요"를 띄운다. 결과가 나오면
  바로 내려받는 것이 안전하다.
- **한 번에 한 장씩** 처리한다. 앞에 대기가 있으면 그만큼 더 걸린다.

---

## 2. 파이프라인 (v7.9.1)

```
사진 ─▶ S1 정규화·격리·크롭
     ─▶ S2 파트 분해        사내 SAM 3.1 파트 세그 (없는 카테고리만 GPT 구성품 분해)
     ─▶ S3 파트별 가시 마스크  세그 마스크 그대로
     ─▶ S4 플랫 스케치 생성   Qwen-Image-Edit-2511 + 사내 도면 LoRA
     ─▶ S5 잉크 증거 추출    성분별 폭·골격·교차·구멍·윤곽
     ─▶ S6 표현 라우팅       선/면/패턴/기하 프리미티브 중 무엇으로 그릴지
     ─▶ S6.5 정리          접합 고리 복원 · 잔가시 재판정 · 글자 간격 · 겹선 정리 (v7.9)
     ─▶ S7 SVG 3종 + .ai 3벌
     ─▶ S8 3중 QA           충실도 · 편집성 · 의미 (+ 연속성·검토 후보 기록)
```

### 어떤 모델을 쓰는가

**전부 RebuilderAI 사내 자산이거나, 사내 워커가 실제로 쓰는 것과 같은 레시피다.**

| 단계 | 모델 | 출처 |
| --- | --- | --- |
| 파트 세그 | **SAM 3.1 카테고리 패키지** — 신발 27 · 가방 36 · 주얼리 14 · 상의 19 · 하의 15 클래스 | `vringon-ai-workers-services` 의 `common.segmentation-sam3`. 가중치는 사내 S3, 런타임은 워커 저장소 `vendor/`. **워커 저장소는 읽기만 했고 수정하지 않았다** |
| 도면 생성 | **Qwen-Image-Edit-2511 + 사내 도면 LoRA**(강도 1.2, seed 484861632801927, 긴 변 1024 · `match_input_image`) | 사내 워커 `generate_schematic_image` 와 같은 레시피. 워커도 Replicate 를 거치므로 우리도 같은 경로를 직접 부른다 |
| 파트 마스크 보정 | Gemini 폴리곤 + SAM 박스 프롬프트 | 도면 위에서 경계를 다시 잡는다 |
| 잔여 잉크 배정 | GPT 비전 | 주인 없는 잉크 덩어리의 **정체만** 묻는다(경계는 잉크가 이미 갖고 있다) |

세그 모델은 H100 서버에 상주 서버로 띄워 HTTP 로 부른다(`tools/h100/partseg_worker.py`,
`VRINGON_SEG_URL`·`VRINGON_SEG_KEY`). 패키지가 없는 카테고리나 워커가 죽으면 **자동으로 기존
경로**(GPT 구성품 분해 + 일반 SAM)로 떨어지고, 로그에 `사내 세그 실패` 한 줄을 남긴다.

### 왜 도면을 파트별로 따로 만들지 않는가

도면 생성은 제품 **전체에 한 번만** 한다. 파트별로 잘라 생성하면 (a) 모델이 매번 형상을 조금씩
다시 그려 파트 경계에서 기하가 안 맞고, (b) 호출 수가 파트 수만큼 늘고, (c) 끈·체인 같은 가는
파트는 맥락 없이 지어낸다. 대신 사진에서 얻은 파트 마스크를 도면 좌표로 옮겨 파트별로 벡터화한다.

---

## 3. 지금 성적

개발용 9종(신발·가방·주얼리 각 3)에서 잰다. 데모에 거는 넷은 이 중 일부다.

| 판 | 앵커 합계 | 평균 선 F@2 | 비고 |
| --- | --- | --- | --- |
| 구 기준선 (면 있음·두꺼운 선) | 25,448 | 0.9832 | |
| v7.0 얇은 마감 | 19,474 | 0.9822 | |
| v7.2 dev 워커 기하 · 가지 붙임 | 17,694 | 0.9840 | |
| v7.4 세그 우선 | 17,249 | 0.9830 | 의미 게이트 +1 |
| v7.5 겹선·티끌 정리 | 15,928 | 0.9826 | 패스 −18% |
| v7.7 연결선 보호 · 연속성 감사 | 15,968 | 0.9828 | |
| v7.9 접합 고리 복원 | (9종 미측정) | | 데모 4종으로만 재고 배포 — shoe_3 회귀를 놓쳤다 |
| **v7.9.1 (현재)** | **13,582** | **0.9873** | 티끌 제거 기본 끔 — 앵커와 충실도가 처음으로 함께 개선 |

**구 기준선 대비 앵커 −47%**, 충실도는 게이트(0.95) 위에서 유지.

3중 게이트는 27개 중 **21개 통과**. 나머지 6은 의미 게이트(파트별 IoU·recall)이고, 원인은
마스크 품질과 사진↔도면 정합이다 — 벡터화의 문제가 아니다.

**연속성**(v7.7 에 새로 만든 자): 긴 획(40px+) 끝점의 **37%가 떠 있다**(9종 1,027/2,802).
이것이 사용자가 "라인이 깨진다"고 말하는 것의 실체이고, 다음 단계의 주 과제다(6절).

**속도**: 한 장 324 → **181초**(캐시 없는 링, 로컬). 그중 우리 코드는 47초이고 나머지 134초는
Replicate 도면 생성이다. 운영에서는 Replicate 콜드 부팅 때문에 회당 107~187초로 흔들려
한 장이 2~4분이 된다.

---

## 4. 개발 방법론

이 프로젝트에서 지켜 온 규칙들이다. 대부분 **한 번 크게 틀린 뒤에** 생겼다.

### 4-1. 게이트가 판단을 대신한다

바꿀 때마다 3중 QA 를 돌리고, **게이트를 넘지 못하면 채택하지 않는다.**

| 게이트 | 무엇을 보나 |
| --- | --- |
| 충실도 | 선 F@2 ≥ 0.95(해프톤 제외) · 작은 디테일 회수율 · 잉크 비율 |
| 편집성 | 앵커 수 · 패스 수 · 파일 크기 |
| 의미 | 파트마다 마스크가 제 잉크를 갖고 있나(IoU · recall) |

"좋아 보인다"로 채택하지 않는다. 눈으로 좋아 보이는데 게이트가 떨어지면 게이트를 믿는다 —
반대로 게이트가 통과인데 눈에 거슬리면, **그때는 자가 부족한 것**이라 자를 새로 만든다(4-4).

### 4-2. A/B 는 도면을 못 박고 잰다

도면 생성 모델은 같은 사진에도 매번 조금씩 다르게 그린다. 그 편차가 알고리즘 효과보다 커서,
못 박지 않으면 **아무 결론도 못 낸다.**

```bash
npx tsx server/demo-cli.ts run <사진> <출력> --category <힌트> --schematic-from <고정 도면>
```

`--schematic-from` 으로 도면을 고정하고, `categoryHint` 를 이전 실행과 같게 맞추고,
`V4_BG_FLATTEN=0` 으로 전처리도 고정한다. 이걸 안 지켜서 초기 v7.2 측정이 한 번 통째로 오염됐다.

### 4-3. 캐시 상태가 같은지 사람이 확인한다

파이프라인에는 외부 호출 캐시가 여럿 있다(도면 · 세그 · 파트 계획 · 잔여 배정 · 글자 인식).
**한쪽만 캐시를 맞으면 그건 대조군이 아니다.**

실측: 잔여 잉크 GPT 배정만 캐시가 없어서, 같은 입력에도 실행마다 답이 달라 jewelry_1 앵커가
383↔404 를 오갔다. 그 잡음이 섞인 채로 "−0.12% 개선"을 보고했다가 외부 리뷰 v0.5 의 지적으로
잡았다. 지금은 그 호출도 캐시하고, **로그의 `캐시 재사용` 문구가 양쪽에서 같은지 눈으로 대조한
뒤에만** 결론을 낸다.

### 4-4. 지표를 늘려 잡는다

하나의 지표만 보면 놓친다.

- 겹선 정리 1차판은 **선 F@2 가 −0.002 뿐**이라 괜찮아 보였는데, **작은 디테일 회수율이
  0.94 → 0.81** 로 무너져 게이트가 깨졌다. F@2 만 봤으면 그대로 배포했을 것이다.
- 사용자가 "라인이 깨진다"고 했을 때 F@2 는 0.98 이었다. 픽셀 일치도로는 연속성을 못 잰다 —
  그래서 `continuityAudit.ts` 를 새로 만들었다(자유끝 · 긴 획 자유끝 · 갈라진 이음).
- v7.9 병합에서 **F@2 가 오르고 앵커가 줄었는데도 회귀**가 있었다: 외곽 상한이 굵기 사다리를
  1.4px 로 뭉개 도면이 평평해졌다. 어느 게이트에도 안 걸렸고 나란히 놓고 봐야 보였다.
  그래서 규칙을 하나 보탠다 — **표시 속성(굵기·색·순서)은 지표가 아니라 그림으로 검사한다.**

### 4-4-1. 검증 표본을 데모 표본으로 줄이지 않는다

데모에는 샘플 넷만 걸지만 **검증은 9종 전부**로 한다. v7.9 를 4종으로만 재고 배포했더니, 빠진
다섯 중 shoe_3 에 충실도 게이트를 깨는 회귀가 있었다(레이스 눈금 3개 소실). 데모 구성과 검증
표본은 별개의 결정이다.

### 4-4-2. 손으로 맞추는 사본은 검사로 바꾼다

`server/v4/version.ts` 의 `CODE_VERSION` 은 실행에 영향을 주는 변경을 할 때 **손으로 올린다**.
올리는 것은 손이지만, **몇 군데를 올려야 하는지는 손에 맡기지 않는다.**

v7.9.1 에서 정리 단계를 고치고 `run4.ts` 의 판만 올렸더니, 한 번의 실행이 낸 scene.json 안에서
`run` 은 v7.9.1, `cleanup` 은 v7.9 로 찍혔다 — 하필 이번에 고친 단계 쪽이 틀렸다. 세어 보니
판 문자열을 손으로 들고 있는 자리가 넷이었다(run4·cleanFinish·stageManifest·editorImport).
"바꾸면 여기도 올린다"는 규칙은 `run4.ts` 주석에 이미 적혀 있었고, 나는 그 주석을 읽고
그 파일만 고쳤다. **사본을 손으로 맞추는 규칙은 지켜지지 않는다.**

넷을 `version.ts` 하나에서 가져오게 하고, `tests/version.test.ts` 로 `server/v4/` 에 판 리터럴이
다시 생기면 파일·줄 번호를 찍고 실패하게 했다. 규칙을 문서에 적는 것과 검사로 만드는 것은
다르다 — 이 규칙은 문서에 적혀 있었는데도 깨졌다.

### 4-5. 외부 리뷰는 유효성과 **규모**를 먼저 잰다

리뷰는 보통 특정 시점의 코드를 본다. 받으면 순서가 이렇다:

1. 지적이 **지금 코드에도** 유효한가 (보관본 기준이면 이미 고쳐졌을 수 있다)
2. 유효하다면 **얼마나 큰가** — 실제로 몇 개인가
3. 그 규모에 맞는 크기로 고친다

실측: 리뷰 v0.6 의 headline 알고리즘(다단 연결선)이 실제로 살린 것은 shoe_1 에서 **7개**였고,
정작 볼륨은 곁다리로 언급된 길이 필터 문제의 **535개**였다. 반대로 리뷰 v0.6 의 두 번째 지적
(잔여 잉크가 면으로 빠져 선이 쪼개진다)은 재 보니 지금 코드에서 소수(긴 자유끝 130개 중 12개)라
**제안된 무거운 모듈을 넣지 않았다.**

### 4-6. 되돌릴 수 있게 만든다

새 동작은 환경변수로 끌 수 있게 둔다. 회귀 비교와 사고 대응이 즉시 가능해진다.

| 변수 | 끄면 |
| --- | --- |
| `V4_MULTIHOP=0` | 다단 연결선 보호 |
| `V4_JUNCTION_REPAIR=0` | 접합 고리 자동 복원 (v7.9) |
| `V4_REVIEW_BLOCKS_GATE=1` | 검토 후보가 편집성 게이트를 가르게 (v7.9 원안) |
| `V4_CLEAN_CAP_GRADED=1` | 표시용 굵기 등급도 1.4px 로 상한 (v7.9 원안) |
| `V4_CLEAN_SPECK_PX=<px>` | 티끌 제거를 **켠다** (기본 끔 — 작은 디테일과 안 갈린다) |
| `V4_LINE_MERGE=0` | 파트 경계 넘는 획 병합 |
| `V4_PRUNE=0` | 겹선·티끌 정리 |
| `V4_GAP=0` | 증거 기반 먼 틈 잇기 |
| `V4_BRIDGE_CUBIC=0` | 접선 큐빅 다리(직선으로) |
| `--no-seg-first` | 사내 세그 우선(GPT 파트 계획으로) |
| `OPENAI_RESIDUAL_EFFORT` | 잔여 배정 추론 강도 |

### 4-7. 상수 옆에 실측을 적는다

코드의 숫자마다 **왜 그 값인지, 무엇을 시도해 실패했는지**를 주석에 남긴다. 예:

> 거리 허용 — 주인 선의 반폭 + 1px. 굵기 합의 0.8배(≈3~5px)로 잡았더니 선 옆의 별개
> 잉크(스티치 점·작은 디테일)까지 삼켰다.

이게 없으면 6개월 뒤에 같은 실패를 반복한다. 실제로 압축 가설은 두 번 반증됐고, 두 번째는
주석 덕분에 빨리 접었다.

### 4-8. 실패한 실험도 남긴다

`docs/V6.md` 에는 채택한 것뿐 아니라 **반증된 것**도 적는다. 최근 것들:

| 시도 | 결과 |
| --- | --- |
| 정합 기준 상자를 세그 전경으로 | bag_1 앵커 742 → 1,259 로 **나빠짐** — 도면 쪽은 배경 검출로 재므로 잣대가 어긋난다. 반사상을 지운 경우로만 좁힘 |
| 짧은 연결선을 전부 보존 | 앵커 +12%, "갈라진 이음" 2배 — 접점 **사이 경로**에 놓인 것만으로 좁힘 |
| 고립 티끌 자동 제거 | 작은 디테일과 기하로 못 가름 — 껐다 |
| 솎기 허용 1.5 → 2.0 / 3.0 | shoe_1 −14 / +4 — 이미 소진된 지렛대 |

### 4-9. 앵커 감소를 목표로 삼지 않는다

선을 지우면 앵커는 준다. 그래서 앵커는 **충실도 게이트를 통과한 상태에서만** 의미가 있다.
v7.3 은 앵커가 사실상 0 이었지만(+0.02%) 채택했다 — 발동한 3곳이 실제로 끊긴 선을 되살렸고
오연결이 0 이었기 때문이다.

---

## 5. 코드 지도

```
server/
  demo-cli.ts              CLI 진입점 (한 장 돌리기)
  index.ts                 HTTP 서버
  config.ts                환경변수 한 곳
  v4/                      현재 파이프라인 (47개)
    run4.ts                오케스트레이터 — S1~S8 + CODE_VERSION
    segVringon.ts          사내 SAM 3.1 파트 세그 (세그 우선)
    segSchematic.ts        도면 직접 세그 (Gemini 폴리곤)
    segSam3.ts             SAM 박스 프롬프트
    residualAssign.ts      주인 없는 잉크의 정체를 GPT 에 질문
    evidence.ts            잉크 증거 (성분별 폭·골격·교차·구멍)
    scene.ts               표현 라우팅 → VectorScene
    router.ts              성분 → 선/면/패턴/기하 판정
    bridgeGaps.ts          끝점 잇기 · 가지 붙임 · 이탈 재는 솎기
    gapBridge.ts           증거 기반 먼 틈 잇기
    pruneStrokes.ts        겹선·티끌 정리
    continuityAudit.ts     연속성 감사 (자유끝 · 갈라진 이음)
    cleanFinish.ts         정리 단계 진입점 — 접합 복원·잔가시·글자 간격을 순서대로 (v7.9)
    junctionRepair.ts      세 갈래 접합의 주선 쌍을 골라 공통 접점으로 복원 (v7.9)
    cleanContours.ts       주선 병합 후 잔가시 재판정 · 짧은 틈 · 접선 정렬 (v7.9)
    glyphClearance.ts      글자 분리가 굵은 획을 지우던 것 교정, 최종 렌더로 검사 (v7.9)
    lineQuality.ts         남은 고리·틈 검토 후보 (v7.9)
    atomicMemo.ts          잔여 배정 캐시를 원자적으로 (v7.9)
    stageManifest.ts       단계별 원본 이미지·해시 추적 (v7.9)
    chainLinks.ts          체인 고리 승격
    qa4.ts                 3중 게이트
    export.ts / aiExport.ts  SVG · .ai 굽기
    tools/                 진단 도구 (연속성·앵커 분포·비교 등)
  vector/
    centerline.ts          골격 → 중심선 체인
    connectors.ts          다단 연결선 보호 (block-cut tree)
    junctionTopology.ts    접점 그래프 · 순환·작은 면 검사 (v7.9)
    fitSpline.ts           접점을 고정한 스플라인 피팅 (v7.9)
    curveGeometry.ts       곡선 표본·투영·호길이 (v7.9)
    fitCurve.ts            곡선 맞춤
    pathdata.ts            SVG path 파싱·직렬화
  v3/                      도면 클라이언트 · 파트 계획 · 정합 · 지표
  v2/                      마스크 · 래스터 유틸
  clients/                 OpenAI · Gemini · fal · Replicate
tools/h100/
  partseg_worker.py        세그 모델 상주 서버 (H100)
  fetch_pkgs.sh            S3 → H100 가중치 내려받기
docs/
  V6.md                    실측 이력 전문 (§1~§21) ← 가장 중요한 문서
  openapi.yaml             API 명세
  plans/V50_METHOD.md      V5.0 방법론
```

---

## 6. 돌려 보기

```bash
npm ci
cp .env.example .env        # 키를 채운다
npx tsx server/demo-cli.ts run <사진.png> <출력폴더> --category jewelry
```

필요한 키(없으면 그 단계만 막히고 나머지는 돈다):

| 변수 | 없으면 |
| --- | --- |
| `REPLICATE_API_TOKEN` 또는 `VRINGON_SCHEMATIC_URL` | 도면 생성 불가 (필수) |
| `OPENAI_API_KEY` | 파트 계획 · 잔여 잉크 배정 불가 |
| `GEMINI_API_KEY` | 도면 직접 세그 불가 (워프 마스크로 진행) |
| `VRINGON_SEG_URL` · `VRINGON_SEG_KEY` | 세그 우선 꺼짐 → GPT 파트 계획으로 |
| `SAM3_SSH_HOST` | H100 일반 SAM 대신 fal.ai |

진단 도구:

```bash
npx tsx server/v4/tools/continuity.ts <출력>/scene.json    # 연속성
npx tsx server/v4/tools/connectortest.ts                   # 연결선 단위시험
```

---

## 7. 남은 과제 (우선순위 순)

1. **긴 획 자유끝 37%** — 연결선 복구로는 거의 안 줄었고, 면에 먹힌 것도 소수다. 남은 후보는
   (a) 도면 자체의 틈과 (b) 원래 거기서 끝나는 선. **둘을 가르려면 "이 경계는 이어져야 한다"는
   정답 세트가 필요하다.** 사람이 링·신발 각 1점에 표시하는 것이 전제다.
2. **도면 생성 속도** — 운영 한 장의 82%가 Replicate 콜드 부팅이다. H100 에 Qwen-Image-Edit
   상주 서버를 세그 워커처럼 올리면 회당 30~60초로 안정되고, 한 장이 1분 30초 안팎이 된다.
3. **사진↔도면 파트별 국소 정합** — 지금은 등방 유사변환 하나라, 도면이 형상을 다시 그린 만큼은
   못 따라간다(jewelry_3 마스크 정합 0.94 → 0.89).
4. **질감 제품의 앵커** — bag_3 는 남은 앵커의 23%가 크로셰 질감 윤곽이다. 선의 문제가 아니라
   표현의 문제라, 모티프 인스턴스·톤 치환으로 접근해야 한다.
5. **곡선 맞춤기 교체** — 긴 선의 앵커 밀도(100px 당 1.6~2.3)를 더 줄이려면 전역 최소제곱 큐빅과
   코너 검출이 있는 맞춤기가 필요하다. 솎기 허용을 올리는 길은 소진됐다.
